name: build

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Set up JDK 1.8
      uses: actions/setup-java@v2.3.1
      with:
        java-version: 1.8
    - name: Run test
      run: sbt -mem 4096 clean test fixCheck
